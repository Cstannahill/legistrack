{
  "name": "ingest-eo",
  "version": "1.0.0",
  "description": "Lambda function to ingest executive orders from the Federal Register API into Supabase.",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "zip": "tsup && rm -rf package-lock.json node_modules && npm install --omit=dev && node ../../zip.js . && npm install"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.157",
    "@types/node": "^24.6.2",
    "@types/pg": "^8.15.6",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1"
  }
}
